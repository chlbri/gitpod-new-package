name: Publish
on: 
  workflow_call:
    secrets:
      token:
        required: true

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
     
    steps:
      - name: Publish
        uses: JS-DevTools/npm-publish@v1
        with:
          token: ${{ secrets.token }}
          greater-version-only: true

      - name: TODO => Notification
        run: echo 'TODO => Build a Notification'
        